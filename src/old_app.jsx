import { useState } from "react"; const US_STATES = ["AL", "AK", "AZ", "AR", "CA", "CO", "CT", "DE", "FL", "GA", "HI", "ID", "IL", "IN", "IA", "KS", "KY", "LA", "ME", "MD", "MA", "MI", "MN", "MS", "MO", "MT", "NE", "NV", "NH", "NJ", "NM", "NY", "NC", "ND", "OH", "OK", "OR", "PA", "RI", "SC", "SD", "TN", "TX", "UT", "VT", "VA", "WA", "WV", "WI", "WY"]; const STEPS = 5; function App() { const [step, setStep] = useState(1); // step 1 = landing, step 2 = form const [selection, setSelection] = useState("select"); // dropdown value const [street, setStreet] = useState(""); const [city, setCity] = useState(""); const [usState, setUsState] = useState(""); const [zip, setZip] = useState(""); const [flightNumber, setFlightNumber] = useState(""); const [passengerCount, setPassengerCount] = useState(""); const [bagsCount, setBagsCount] = useState(""); const [fullName, setFullName] = useState(""); const [phone, setPhone] = useState(""); const [email, setEmail] = useState(""); // Handle moving to next step const handleNext = () => { if (selection !== "select") { setStep(2); } }; return ( <div className="w-full flex items-center justify-center bg-white text-gray-800 min-h-screen p-6"> <div className="max-w-md mx-auto p-4 sm:p-6 text-center border rounded-lg shadow-md bg-gray-50"> {step === 1 && ( <> <h1 className="text-4xl font-bold mb-6">Welcome to Infamous EV</h1> <p className="text-lg mb-8"> Schedule a ride to your destination with just a few taps. </p> <button className="bg-red-600 text-white text-lg font-semibold py-3 px-6 rounded-lg hover:bg-red-700 transition" onClick={() => setStep(2)} > Book a Reservation </button> </> )} {step === 2 && ( <> <h1 className="text-2xl font-bold mb-6">Book a Reservation ({step-1}/{STEPS})</h1> <h2 className="text-2xl font-semibold text-center">Choose an Option</h2> <div className="flex-1 flex items-center justify-center p-4"> <select className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white" value={selection} onChange={(e) => setSelection(e.target.value)} > <option value="select">Select an option</option> <option value="pickup">Pick up at airport</option> <option value="dropoff">Drop off at airport</option> </select> </div> <div className="w-full max-w-md mx-auto flex gap-4 mt-4"> <button className="flex-1 py-3 rounded-lg text-gray-700 bg-gray-200 hover:bg-gray-300 transition" onClick={() => setStep(1)} > Back </button> <button className={flex-1 py-3 rounded-lg text-white font-semibold transition ${ selection === "select" ? "bg-gray-400 cursor-not-allowed" : "bg-red-600 hover:bg-red-700" }} disabled={selection === "select"} onClick={() => setStep(3)} // later goes to Step 3 > Next </button> </div> </> )} {step === 3 && ( <div className="w-full h-screen flex flex-col justify-between bg-white text-gray-800 p-6"> {/* Header */} <h2 className="text-2xl font-bold text-center mb-6"> {selection === "pickup" ? "Destination" : "Pickup Address"} ({step-1}/{STEPS}) </h2> {/* Form Fields */} <div className="flex-1 flex flex-col justify-center max-w-md mx-auto w-full space-y-4"> <input type="text" placeholder="Street Address" value={street} onChange={(e) => setStreet(e.target.value)} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> <input type="text" placeholder="City" value={city} onChange={(e) => setCity(e.target.value)} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> <select value={usState} onChange={(e) => setUsState(e.target.value)} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white" > <option value="">Select State</option> {US_STATES.map((abbr) => ( <option key={abbr} value={abbr}> {abbr} </option> ))} </select> <input type="text" placeholder="Zip Code" value={zip} onChange={(e) => setZip(e.target.value)} maxLength={5} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> </div> {/* Buttons */} <div className="w-full max-w-md mx-auto flex gap-4 mt-4"> <button onClick={() => setStep(2)} className="flex-1 py-3 rounded-lg text-gray-700 bg-gray-200 hover:bg-gray-300 transition" > Back </button> <button disabled={ !street.trim() || !city.trim() || !usState || !/^\d{5}$/.test(zip) // must be 5 digits } onClick={() => setStep(4)} // later goes to Step 4} className={flex-1 py-3 rounded-lg text-white font-semibold transition ${ !street.trim() || !city.trim() || !/^\d{5}$/.test(zip) ? "bg-gray-400 cursor-not-allowed" : "bg-red-600 hover:bg-red-700" }} > Next </button> </div> </div> )} {step === 4 && ( <div className="w-full h-screen flex flex-col justify-between bg-white text-gray-800 p-6"> {/* Header */} <h2 className="text-2xl font-bold text-center mb-6">Flight Details ({step-1}/{STEPS})</h2> {/* Form Fields */} <div className="flex-1 flex flex-col justify-center max-w-md mx-auto w-full space-y-4"> <input type="text" placeholder="Flight Number" value={flightNumber} onChange={(e) => setFlightNumber(e.target.value)} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> <input type="number" placeholder="Passenger Count" value={passengerCount} onChange={(e) => setPassengerCount(e.target.value)} min={1} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> <input type="number" placeholder="Bags Count" value={bagsCount} onChange={(e) => setBagsCount(e.target.value)} min={0} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> </div> {/* Buttons */} <div className="w-full max-w-md mx-auto flex gap-4 mt-4"> <button onClick={() => setStep(3)} className="flex-1 py-3 rounded-lg text-gray-700 bg-gray-200 hover:bg-gray-300 transition" > Back </button> <button disabled={ !flightNumber.trim() || !passengerCount || !bagsCount } onClick={() => setStep(5)} // later goes to Step 5 //alert(Flight: ${flightNumber}, Passengers: ${passengerCount}, Bags: ${bagsCount}) className={flex-1 py-3 rounded-lg text-white font-semibold transition ${ !flightNumber.trim() || !passengerCount || !bagsCount ? "bg-gray-400 cursor-not-allowed" : "bg-red-600 hover:bg-red-700" }} > Next </button> </div> </div> )} {step === 5 && ( <div className="w-full h-screen flex flex-col justify-between bg-white text-gray-800 p-6"> {/* Header */} <h2 className="text-2xl font-bold text-center mb-6">Contact Details ({step-1}/{STEPS})</h2> {/* Form Fields */} <div className="flex-1 flex flex-col justify-center max-w-md mx-auto w-full space-y-4"> {/*<label className="block text-left text-gray-700 mb-1">Full Name</label>*/} <input type="text" placeholder="Full Name" value={fullName} onChange={(e) => setFullName(e.target.value)} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> <input type="tel" placeholder="Phone Number" value={phone} onChange={(e) => setPhone(e.target.value)} className="w-full border border-gray-300 rounded-lg p-3 text-lg focus:ring-2 focus:ring-blue-500 focus:outline-none" /> <input type="email" placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} className="w-full p-4 text-lg border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" /> </div> {/* Buttons */} <div className="w-full max-w-md mx-auto flex gap-4 mt-4"> <button onClick={() => setStep(4)} className="flex-1 py-3 rounded-lg text-gray-700 bg-gray-200 hover:bg-gray-300 transition" > Back </button> <button disabled={ !fullName.trim() || !phone || !email } onClick={() => setStep(6)} // later goes to Step 6 //alert(Flight: ${flightNumber}, Passengers: ${passengerCount}, Bags: ${bagsCount}) className={flex-1 py-3 rounded-lg text-white font-semibold transition ${ !fullName.trim() || !phone || !email ? "bg-gray-400 cursor-not-allowed" : "bg-red-600 hover:bg-red-700" }} > Review </button> </div> </div> )} </div> </div> ); } export default App;